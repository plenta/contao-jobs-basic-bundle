{% extends '@Contao/frontend_module/_base.html.twig' %}

{% block content %}
    {{ content|raw }}

    {% if back|default %}
        <!-- indexer::stop -->
        <p class="back"><a href="{{ referer }}" title="{{ back }}">{{ back }}</a></p>
        <!-- indexer::continue -->
    {% endif %}
{% endblock %}
